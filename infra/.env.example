# ─────────────────────────────────────────────────────────────
#  SOOLEVO.COM — Zmienne środowiskowe
#  SKOPIUJ DO: infra/.env (NIGDY nie commit'uj .env do repo!)
# ─────────────────────────────────────────────────────────────

# ── PostgreSQL ─────────────────────────────────────────────
POSTGRES_DB=soolevo
POSTGRES_USER=soolevo
POSTGRES_PASSWORD=ZMIEN_TO_NA_SILNE_HASLO_32_ZNAKI

# ── Aplikacja ──────────────────────────────────────────────
# Wygeneruj: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=WYGENERUJ_LOSOWY_KLUCZ_64_ZNAKI

# ── mBank PayNow ───────────────────────────────────────────
# Pobierz z: https://panel.paynow.pl → Ustawienia → API
PAYNOW_API_KEY=TWOJ_KLUCZ_API_PAYNOW
PAYNOW_SIGNATURE_KEY=TWOJ_KLUCZ_PODPISU_PAYNOW
PAYNOW_SANDBOX=false
# Cena w groszach (4900 = 49.00 zł)
REPORT_PRICE_GROSZY=4900

# ── Frontend / CORS ────────────────────────────────────────
FRONTEND_URL=https://soolevo.com
ALLOWED_ORIGINS=https://soolevo.com,https://www.soolevo.com

# ── Email (opcjonalne — do wysyłki linku do raportu) ───────
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=twoj@email.pl
SMTP_PASSWORD=HASLO_APLIKACJI_GMAIL
EMAIL_FROM=noreply@soolevo.com
